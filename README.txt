# You Will Need
- Download a public key from your Auth0 app and save it as `public_key` in the project root.
- Configure .env and .env.local files (see sample.env / sample.env.local for info)
- auth0 client id into auth0.json (see sample.auth0.json for info)